{
  "web": {
    "issuer": "${KEYCLOAK_HOSTNAME_URL}/realms/${APP_REALM}",
    "auth_uri": "${KEYCLOAK_HOSTNAME_URL}/realms/${APP_REALM}/protocol/openid-connect/auth",
    "client_id": "${SUPERSET_CLIENT_ID}",
    "client_secret": "${SUPERSET_CLIENT_SECRET}",
    "redirect_uris": ["${SUPERSET_ROOT_URL}/oidc_callback"],
    "userinfo_uri": "${KEYCLOAK_INTERNAL}/realms/${APP_REALM}/protocol/openid-connect/userinfo",
    "token_uri": "${KEYCLOAK_INTERNAL}/realms/${APP_REALM}/protocol/openid-connect/token",
    "token_introspection_uri": "${KEYCLOAK_INTERNAL}/realms/${APP_REALM}/protocol/openid-connect/token/introspect"
  }
}
